export JWT_PUBLIC_KEY=$(lockbox watchers/pubKey)
export TODOIST_KEY=$(lockbox watchers/todoist/token)
export TODOIST_CLIENT_ID=$(lockbox watchers/todoist/clientId)
export TODOIST_CLIENT_SECRET=$(lockbox watchers/todoist/clientSecret)
export TODOIST_REDIRECT_URL="https://todoist.qnzl.now.sh/api/"
export ISSUER="https://qnzl.co"

